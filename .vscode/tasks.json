{
  "tasks": [
    {
      "command": "pipenv install --dev",
      "label": "pipenv-install-dev",
      "type": "shell"
    },
    {
      "command": "pipenv run python ./manage.py migrate",
      "dependsOn": [
        "pipenv-install-dev"
      ],
      "label": "migrate-db",
      "type": "shell"
    }
  ],
  "version": "2.0.0"
}